# =============================================================================
# Spark Test Mode (Spark-only, DMZ-safe)
# =============================================================================
#
# Purpose:
# - Dedicated test environment on Spark (no Mac dependency)
# - Uses Spark-local services inside Docker network
# - Safe for pytest/integration checks in DMZ scenarios
#
# Usage:
#   cp profiles/spark_test.env shared-config.env
#   git add shared-config.env && git commit -m "Switch to spark test mode"
#
# =============================================================================

# Mode identification
POMSPARK_MODE=test
ENVIRONMENT=test
PYTEST_RUNNING=true

# Weaviate routing (Spark-local)
WEAVIATE_MODE=spark
WEAVIATE_URL=http://spark-weaviate:8080
SPARK_WEAVIATE_URL=http://spark-weaviate:8080
WEAVIATE_GRPC_URL=spark-weaviate:50051
SPARK_WEAVIATE_GRPC_URL=spark-weaviate:50051

# Redis (observability + cache)
REDIS_URL=redis://spark-redis:6379

# Ollama (Spark GPU)
OLLAMA_URL=http://spark-ollama:11434
OLLAMA_BASE_URL=http://spark-ollama:11434
SPARK_OLLAMA_URL=http://spark-ollama:11434

# Transformers (Spark GPU)
TRANSFORMERS_URL=http://spark-transformers-lb:80
SPARK_TRANSFORMERS_URL=http://spark-transformers-lb:80

# Shared config mount
POM_CONFIG_ROOT=/app/shared_config
