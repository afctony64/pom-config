# =============================================================================
# SPARK TEST MODE PROFILE
# =============================================================================
#
# Use when: Running Spark-only tests in DMZ or isolated environments
#
# This profile configures the ecosystem to use:
#   - Spark-local Weaviate, Ollama, Transformers, and Redis
#   - Test flags for pom-core integration tests
#
# To activate: Copy values to shared-config.env or source this file
#
# =============================================================================

# Mode Identifier
POMSPARK_MODE=test

# Test flags
ENVIRONMENT=test
PYTEST_RUNNING=true

# =============================================================================
# WEAVIATE CONFIGURATION
# =============================================================================

# Primary Weaviate mode (Spark local)
WEAVIATE_MODE=spark

# Weaviate URLs (Spark-only, Docker network)
WEAVIATE_URL=http://spark-weaviate:8080
SPARK_WEAVIATE_URL=http://spark-weaviate:8080
WEAVIATE_GRPC_URL=spark-weaviate:50051

# Default target for new collections
DEFAULT_WEAVIATE_TARGET=spark

# =============================================================================
# OLLAMA CONFIGURATION (LLM Inference)
# =============================================================================

# Primary Ollama URL (Spark Docker - CUDA GPU)
OLLAMA_URL=http://spark-ollama:11434
OLLAMA_BASE_URL=http://spark-ollama:11434
SPARK_OLLAMA_URL=http://spark-ollama:11434

# =============================================================================
# TRANSFORMERS CONFIGURATION (Embeddings)
# =============================================================================

# Primary Transformers URL (Spark load balancer - GPU cluster)
TRANSFORMERS_URL=http://spark-transformers-lb:80
SPARK_TRANSFORMERS_URL=http://spark-transformers-lb:80

# =============================================================================
# OTHER SERVICES
# =============================================================================

REDIS_URL=redis://spark-redis:6379
