# =============================================================================
# TRAVEL MODE PROFILE
# =============================================================================
#
# Use when: Only Mac is available (traveling, remote work)
#
# This profile configures the ecosystem to use:
#   - Mac native Ollama (Metal GPU acceleration)
#   - Mac native Transformers (Metal acceleration)
#   - Mac Weaviate for local collections
#   - Cloud services for anything requiring GPU power
#
# To activate: Copy values to shared-config.env or source this file
#
# =============================================================================

# Mode Identifier
POMSPARK_MODE=travel

# =============================================================================
# WEAVIATE CONFIGURATION
# =============================================================================

# Primary Weaviate mode (Mac local)
WEAVIATE_MODE=local

# Weaviate URLs (Mac only)
MAC_WEAVIATE_URL=http://mac-weaviate:8080
WEAVIATE_URL=http://mac-weaviate:8080

# Spark not available in travel mode
# SPARK_WEAVIATE_URL=http://spark-weaviate:8080  # Not available

# Default target for new collections (prefer cloud when traveling)
DEFAULT_WEAVIATE_TARGET=cloud

# =============================================================================
# OLLAMA CONFIGURATION (LLM Inference)
# =============================================================================

# Primary Ollama URL (Mac native - Metal GPU)
# Docker containers access via host.docker.internal
OLLAMA_URL=http://host.docker.internal:11435
MAC_OLLAMA_URL=http://host.docker.internal:11435

# Spark not available in travel mode
# SPARK_OLLAMA_URL=http://spark-ollama:11434  # Not available

# =============================================================================
# TRANSFORMERS CONFIGURATION (Embeddings)
# =============================================================================

# Primary Transformers URL (Mac native - Metal)
TRANSFORMERS_URL=http://host.docker.internal:8093
MAC_TRANSFORMERS_URL=http://host.docker.internal:8093

# Spark not available in travel mode
# SPARK_TRANSFORMERS_URL=http://spark-transformers-lb:80  # Not available

# =============================================================================
# LLM PROXY CONFIGURATION
# =============================================================================

LLM_PROXY_URL=http://pom-llm-proxy:8000

# =============================================================================
# OTHER SERVICES
# =============================================================================

REDIS_URL=redis://mac-redis:6379

# =============================================================================
# TRAVEL MODE NOTES
# =============================================================================
#
# Performance expectations in travel mode:
#   - Ollama: ~87 tok/s (Metal) vs ~500 tok/s (Spark CUDA)
#   - Embeddings: Slower but functional
#   - Heavy workloads: Consider using cloud LLMs
#
# Start native services before Docker:
#   ./scripts/mac-ollama-metal.sh start
#   ./scripts/mac-transformers-metal.sh start
#
# =============================================================================
