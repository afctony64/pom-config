# =============================================================================
# HOME MODE PROFILE
# =============================================================================
#
# Use when: Mac and Spark server are both available (office/home)
#
# This profile configures the ecosystem to use:
#   - Spark GPU for Ollama (fast inference)
#   - Spark GPU for Transformers/embeddings
#   - Spark Weaviate for local collections
#   - Mac for development containers
#
# To activate: Copy values to shared-config.env or source this file
#
# =============================================================================

# Mode Identifier
POMSPARK_MODE=home

# =============================================================================
# WEAVIATE CONFIGURATION
# =============================================================================

# Primary Weaviate mode (where to route queries)
WEAVIATE_MODE=spark

# Weaviate URLs
MAC_WEAVIATE_URL=http://mac-weaviate:8080
SPARK_WEAVIATE_URL=http://spark-weaviate:8080
WEAVIATE_GRPC_URL=spark-weaviate:50051

# Default target for new collections
DEFAULT_WEAVIATE_TARGET=cloud

# =============================================================================
# OLLAMA CONFIGURATION (LLM Inference)
# =============================================================================

# Primary Ollama URL (Spark Docker container - CUDA GPU)
OLLAMA_URL=http://spark-ollama:11434
SPARK_OLLAMA_URL=http://spark-ollama:11434

# Mac native Ollama (fallback or for Mac-only operations)
# Note: Docker containers access via host.docker.internal
MAC_OLLAMA_URL=http://host.docker.internal:11435

# =============================================================================
# TRANSFORMERS CONFIGURATION (Embeddings)
# =============================================================================

# Primary Transformers URL (Spark load balancer - GPU cluster)
TRANSFORMERS_URL=http://spark-transformers-lb:80

# Mac Metal transformers (fallback)
MAC_TRANSFORMERS_URL=http://host.docker.internal:8093

# =============================================================================
# LLM PROXY CONFIGURATION
# =============================================================================

LLM_PROXY_URL=http://pom-llm-proxy:8000

# =============================================================================
# OTHER SERVICES
# =============================================================================

REDIS_URL=redis://mac-redis:6379
