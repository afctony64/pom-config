# Tenant Routing Configuration (Deprecated)

Mode-based tenant routing has been removed. Service discovery is dynamic and
tenant routing should not be defined per "home/travel/ooo" modes.

Do not reintroduce mode-specific routing files in this directory.
